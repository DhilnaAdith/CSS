//total expense, largest and smallest expense

var expense = [1000,1500,2400,3000,4000]

// total expense
total=0
for(let amount of expense)
{
    totel+=amount
}
console.log("total expense is :" + total);

//largest amount in expense array
 largest=0
    
